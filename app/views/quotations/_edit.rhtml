<!--
TODO: when category is manually edited, redraw links; 
so if category is manually deleted, the clickable link for it turns blue
-->

<%= start_form_tag({:action => 'edit_quotation'}, { :id => 'quotation_form' }) %>
  <%= render :partial => 'quotations/form' %>
<%= end_form_tag %>